# EcoCarbon Fullstack Backend

## Run
```
cd backend
npm install
npm start
```
API endpoints:
- POST /api/register
- POST /api/login
- GET /api/protected (requires Bearer token)
DB at backend/db/users.db
